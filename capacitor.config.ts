import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nelson.app',
  appName: 'Ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
